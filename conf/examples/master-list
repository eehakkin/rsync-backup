# A sample conf/master-list file
# ==============================

# Mirror remote backup directories.
remote.example.com:/srv/backup/localhost/.

# An alternative sample conf/master-list file
# ===========================================

# Mirror remote backup directories using a different remote user.
backup@remote.example.com:/srv/backup/localhost/.
